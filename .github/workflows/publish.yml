name: Publish symbolic_math_bridge to pub.dev
on:
  push:
    tags: ['symbolic_math_bridge-v[0-9]+.[0-9]+.[0-9]+']
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # OIDC auth to pub.dev (no stored secrets)
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - name: Publish
        working-directory: .
        run: |
          flutter pub get
          flutter pub publish --force
