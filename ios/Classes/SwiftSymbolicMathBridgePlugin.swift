import Flutter
import UIKit

// This class is the required entry point for the Flutter plugin.
public class SwiftSymbolicMathBridgePlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // This plugin's sole purpose is to link the native libraries.
    // It does not need to handle method calls.
  }
}
